server {
    listen 9000;

    client_max_body_size 0;

    location / {
        proxy_pass http://feed-service-minio:9000;

        proxy_set_header X-Real-IP $remote_addr;

        proxy_set_header Authorization $http_authorization;
    }
}